// booleans & comparison operator 

// booleans 
// true, false 

//   its pretty simple dude 



let num1 = 7;
let num2 = "7";

// console.log(num1<num2);

// == vs === 
// == in JavaScript is used for comparing two variables, but it ignores the datatype of variable. === is used for comparing two variables, but this operator also checks datatype and compares two values. Checks the equality of two operands without considering their type.

//  so if we compare num1 and num2 , in that case if we use (==) then we are getting true , bcz it dont check the variable , it only check the value  and  if we use (===) , then we are getting everytime false , bcz it check variable and value both 
console.log(num1 === num2);

// != vs !==

// console.log(num1 !== num2);